# IVC

A lightweight, minimalistic image viewer written in C using SDL.

## Features
- Fast and simple image viewing
- Supports common image formats (via SDL_image)
- Single-file launch

## Usage
```bash
ivc [image_file]
